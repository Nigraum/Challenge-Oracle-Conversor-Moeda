package conversor_moedas;
import javax.swing.JOptionPane;

public class Menu {
	
	String[] options = {"Conversor de Moeda", "Conversor de Temperatura"}
	
	String escolha = (String) JOptionPane.showInputDialog(null, "Conversor de Moedas:", "Menu", JOptionPane.PLAIN_MESSAGE, null, opcoes, opcoes[0])
			
	if (escolha != null) {
		switch (escolha) {
		
		}
	}
}
